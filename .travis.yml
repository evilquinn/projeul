
language: cpp

compiler:
    - gcc

addons:
    apt:
        sources:
             - ubuntu-toolchain-r-test
        packages: libboost-all-dev cmake g++-5

script:
    - mkdir build
    - cd build
    - cmake ../.
    - make
    - ./test/test_projeul

